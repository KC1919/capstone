<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>

  <body style="text-align: center; background-color: aliceblue">
    <div style="margin: 30px 0 20px 0">
      <h2>Generate OTP</h2>
    </div>

    <div
      style="
        display: flex;
        flex-direction: column;
        justify-content: space-around;
        align-items: center;
      "
    >
      <div
        style="
          background-color: chartreuse;
          margin: 20px;
          padding: 5rem 23.5rem 5rem 23.5rem;
        "
      >
        <form action="/user/getotp" method="get">
          <input type="submit" class="btn btn-info" value="Get OTP" />
        </form>
      </div>

      <div style="margin: 30px 0 20px 0">
        <h2>Upload File For OTP Verification</h2>
      </div>

      <div style="background-color: cornflowerblue; margin: 20px">
        <form action="/user/upload" method="post" enctype="multipart/form-data">
          <div style="margin: 20px; padding: 5rem 14rem 5rem 14rem">
            <input type="file" name="image" />
            <input class="btn btn-danger" type="submit" value="Verify" />
          </div>
        </form>
      </div>
    </div>

    <!-- <script>
      async function handleGetOtp(e) {
        e.preventDefault();
        const response = await fetch("/user/getotp", {
          method: "GET",
        });

        // const jsonResp = await response.json();

        console.log(response);

        if (jsonResp.success == true) {
          alert("OTP Sent Successfully");
        } else {
          alert("Failed to sent OTP");
          location.reload();
        }
      } -->
    </script>
  </body>
</html>
